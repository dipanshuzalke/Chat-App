{
    "name": "chat-app",
    "version": "1.0.0",
    "description": "Full-stack chat app with WebSocket server and React frontend",
    "main": "index.js",
    "scripts": {
        "start": "concurrently \"cd server && npm run start\" \"cd client && npm run start\"",
        "build": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
        "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
    },
    "dependencies": {
        "concurrently": "^7.6.0"
    },
    "devDependencies": {
        "typescript": "^5.7.2"
    }
}
